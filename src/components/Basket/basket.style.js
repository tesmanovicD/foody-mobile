import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1
    },
    items: {
        backgroundColor: '#fff',
        marginHorizontal: 10,
        marginTop: 15
    }
})